# Register Chef Statistical Functions

Wrapper to access chefStats functions with pharmhand metadata.

## Usage

``` r
get_chef_stat(stat_name)
```

## Arguments

- stat_name:

  Name of the chefStats function

## Value

A function wrapped with metadata for traceability
