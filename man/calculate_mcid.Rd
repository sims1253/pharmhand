% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pro_analysis.R
\name{calculate_mcid}
\alias{calculate_mcid}
\title{Calculate MCID (Combined Approach)}
\usage{
calculate_mcid(
  data,
  score_var,
  anchor_var = NULL,
  baseline_var = NULL,
  method = c("both", "anchor", "distribution"),
  reliability = NULL,
  anchor_minimal = "Minimally Improved",
  conf_level = 0.95
)
}
\arguments{
\item{data}{Data frame with PRO data}

\item{score_var}{Character. Name of the score/change variable}

\item{anchor_var}{Character. Name of anchor variable (required
for anchor method)}

\item{baseline_var}{Character. Name of baseline score variable
(for distribution method)}

\item{method}{Character. "anchor", "distribution", or "both"
(default: "both")}

\item{reliability}{Numeric. Test-retest reliability for SEM calculation}

\item{anchor_minimal}{Character vector. Anchor values for minimal improvement}

\item{conf_level}{Numeric. Confidence level (default: 0.95)}
}
\value{
List containing MCID results. Structure depends on method:
\itemize{
\item If method = "anchor": anchor-based results with mcid, ci, n, method
\item If method = "distribution": distribution-based results
(half_sd, one_sem, etc.)
\item If method = "both": both anchor and distribution results
}
}
\description{
Wrapper function to calculate MCID using anchor-based, distribution-based,
or both approaches.
}
\examples{
\dontrun{
# Anchor-based MCID
anchor_result <- calculate_mcid(
  data = pro_data,
  score_var = "AVAL",
  anchor_var = "PGIC",
  responder_value = "Much improved",
  method = "anchor"
)

# Distribution-based MCID
dist_result <- calculate_mcid(
  data = pro_data,
  score_var = "AVAL",
  method = "distribution",
  reliability = 0.85
)
}
}
