% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efficacy_tables.R
\name{test_non_inferiority}
\alias{test_non_inferiority}
\title{Non-Inferiority Test}
\usage{
test_non_inferiority(
  data,
  outcome_var,
  trt_var,
  ref_group,
  ni_margin,
  type = c("continuous", "binary"),
  higher_better = TRUE,
  conf_level = 0.975,
  method = c("wilson", "wald", "exact")
)
}
\arguments{
\item{data}{Data frame}

\item{outcome_var}{Character. Outcome variable name}

\item{trt_var}{Character. Treatment variable name}

\item{ref_group}{Character. Reference group name}

\item{ni_margin}{Numeric. Non-inferiority margin (positive value)}

\item{type}{Character. "continuous" or "binary"}

\item{higher_better}{Logical. TRUE if higher values are better
(default: TRUE)}

\item{conf_level}{Numeric. One-sided confidence level
(default: 0.975 for 95\% CI)}

\item{method}{Character. For binary: "wald", "wilson", "exact"
(default: "wilson")}
}
\value{
List with:
\itemize{
\item estimate: Point estimate of difference (trt - ref)
\item ci_lower: Lower bound of one-sided CI
\item ci_upper: Upper bound (may be Inf for one-sided)
\item ni_margin: The margin used
\item non_inferior: Logical. TRUE if lower CI > -ni_margin
(or upper < margin if lower is worse)
\item conclusion: Character summary
\item method: Method used
}
}
\description{
Tests non-inferiority of treatment vs comparator.
}
\details{
For continuous endpoints: Tests if (mean_trt - mean_ref) + ni_margin > 0
For binary endpoints: Tests if (prop_trt - prop_ref) + ni_margin > 0

Non-inferiority is concluded if the lower bound of the one-sided CI
for the treatment difference exceeds -ni_margin.

For binary endpoints, the Wilson method uses the Newcombe-Wilson
hybrid CI approach. The Wald method provides a simpler alternative.
For regulatory submissions, verify the CI method aligns with
agency preferences.
}
\references{
IQWiG Methods v8.0, Section 10.3.5, p. 217-218.
}
