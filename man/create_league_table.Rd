% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_network.R
\name{create_league_table}
\alias{create_league_table}
\title{Generates a league table showing all pairwise treatment comparisons
from a network meta-analysis.}
\usage{
create_league_table(
  nma_result,
  digits = 2,
  show_ci = TRUE,
  highlight_sig = TRUE,
  conf_level = NULL,
  theme = c("hta", "iqwig", "gba", "clinical")
)
}
\arguments{
\item{nma_result}{Result from network_meta()}

\item{digits}{Integer. Decimal places for estimates. Default: 2}

\item{show_ci}{Logical. Show confidence intervals. Default: TRUE}

\item{highlight_sig}{Logical. Highlight significant comparisons.
Default: TRUE}

\item{conf_level}{Numeric. Confidence level. If NULL (default), uses the level
from the NMA result or falls back to 0.95.}

\item{theme}{Theme preset: "hta", "iqwig", "gba", or "clinical"
(default: "hta")}
}
\value{
\link{ClinicalTable} with styled league table
}
\description{
Generates a league table showing all pairwise treatment comparisons
from a network meta-analysis.
}
\examples{
# Create league table for NMA
nma_data <- data.frame(
  study = c("S1", "S2", "S3"),
  treat1 = c("A", "B", "A"),
  treat2 = c("B", "C", "C"),
  effect = log(c(0.75, 0.90, 0.80)),
  se = c(0.12, 0.15, 0.18)
)
nma_result <- network_meta(nma_data, effect_measure = "hr")
table <- create_league_table(nma_result)
print(table)
}
