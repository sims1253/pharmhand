% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gba_utils.R
\name{calculate_smd_binary}
\alias{calculate_smd_binary}
\title{Calculate Standardized Mean Difference for Binary Variables}
\usage{
calculate_smd_binary(
  p1,
  n1,
  p2,
  n2,
  method = c("arcsine", "logit", "raw"),
  conf_level = 0.95
)
}
\arguments{
\item{p1}{Numeric. Proportion in group 1 (treatment). Must be 0 to 1.}

\item{n1}{Integer. Sample size of group 1. Must be >= 2.}

\item{p2}{Numeric. Proportion in group 2 (control). Must be 0 to 1.}

\item{n2}{Integer. Sample size of group 2. Must be >= 2.}

\item{method}{Character. Method for calculating SMD. One of:
\itemize{
\item \code{"arcsine"} (default): Uses arcsine square root transformation
\item \code{"logit"}: Uses logit transformation (log odds)
\item \code{"raw"}: Raw proportion difference standardized by pooled variance
}}

\item{conf_level}{Numeric. Confidence level for CI (default: 0.95)}
}
\value{
A named list with components:
\itemize{
\item \code{smd}: The standardized mean difference
\item \code{ci_lower}: Lower bound of confidence interval
\item \code{ci_upper}: Upper bound of confidence interval
\item \code{method}: Method used
\item \code{se}: Standard error of the SMD
}
}
\description{
Calculates the standardized mean difference (SMD) between two groups for
binary or categorical variables using the arcsine transformation or logit
method. This is essential for assessing baseline balance of categorical
covariates in clinical trials.
}
\details{
\strong{Arcsine method} (recommended for proportions):
\deqn{SMD = 2 \times (\arcsin(\sqrt{p_1}) - \arcsin(\sqrt{p_2}))}

This transformation stabilizes variance across the range of proportions
and is bounded, making it suitable for proportions near 0 or 1.

\strong{Logit method} (log odds ratio):
Calculates the log odds ratio and standardizes:
\deqn{SMD = \frac{\log(OR)}{\pi/\sqrt{3}}}

where \eqn{OR = \frac{p_1/(1-p_1)}{p_2/(1-p_2)}}

\strong{Raw method}:
\deqn{SMD = \frac{p_1 - p_2}{\sqrt{p(1-p)}}}

where \eqn{p = \frac{n_1 p_1 + n_2 p_2}{n_1 + n_2}}
}
\examples{
# Compare sex distribution (60\% female in treatment, 55\% in control)
calculate_smd_binary(p1 = 0.60, n1 = 150, p2 = 0.55, n2 = 148)

# Using logit transformation
calculate_smd_binary(
  p1 = 0.60, n1 = 150,
  p2 = 0.55, n2 = 148,
  method = "logit"
)
}
\references{
Austin, P. C. (2009). Balance diagnostics for comparing the distribution of
baseline covariates between treatment groups in propensity-score matched
samples. Statistics in Medicine, 28(25), 3083-3107.
}
\seealso{
\code{\link[=calculate_smd]{calculate_smd()}} for continuous variables,
\code{\link[=calculate_smd_from_data]{calculate_smd_from_data()}} for calculating directly from data
}
