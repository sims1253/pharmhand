% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efficacy_tables.R
\name{test_ph_assumption}
\alias{test_ph_assumption}
\title{Test Proportional Hazards Assumption}
\usage{
test_ph_assumption(
  data,
  time_var = NULL,
  event_var = NULL,
  trt_var = NULL,
  covariates = character(),
  alpha = 0.05,
  plot = FALSE
)
}
\arguments{
\item{data}{Data frame with time-to-event data, or a coxph model object}

\item{time_var}{Character. Time variable (required if data is a data frame)}

\item{event_var}{Character. Event variable (required if data is a data frame)}

\item{trt_var}{Character. Treatment variable (required if data is a
data frame)}

\item{covariates}{Character vector. Additional covariates to include}

\item{alpha}{Numeric. Significance level for flagging violations
(default: 0.05)}

\item{plot}{Logical. Whether to create diagnostic plot (default: FALSE)}
}
\value{
A list with:
\itemize{
\item results: Data frame with variable, rho, chisq, p-value, violation flag
\item global_test: Global test result (p-value)
\item violation: Logical, TRUE if any p < alpha
\item model: The fitted coxph model
\item zph: The cox.zph result object
\item plot: ClinicalPlot if plot=TRUE
}
}
\description{
Tests the proportional hazards assumption for Cox regression models
using Schoenfeld residuals (cox.zph test).
}
\examples{
\dontrun{
result <- test_ph_assumption(
  data = adtte,
  time_var = "AVAL",
  event_var = "CNSR",
  trt_var = "TRT01P",
  plot = TRUE
)
}
}
\references{
Grambsch, P. M. and Therneau, T. M. (1994).
Proportional hazards tests and diagnostics based on weighted residuals.
Biometrika, 81, 515-26.

IQWiG Methods v8.0, Section 10.3.12, p. 235-237.
}
