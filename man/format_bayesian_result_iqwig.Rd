% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_bayesian.R
\name{format_bayesian_result_iqwig}
\alias{format_bayesian_result_iqwig}
\title{Format Bayesian Meta-Analysis Result for IQWiG Submission}
\usage{
format_bayesian_result_iqwig(
  bayesian_result,
  digits_estimate = 3L,
  digits_ci = 3L,
  digits_tau = 3L,
  locale = get_locale(),
  include_prob = TRUE,
  include_interpretation = TRUE,
  ci_brackets = c("[", "]")
)
}
\arguments{
\item{bayesian_result}{A bayesian_meta_result object from
bayesian_meta_analysis()}

\item{digits_estimate}{Integer. Decimal places for estimate (default: 3)}

\item{digits_ci}{Integer. Decimal places for CI bounds (default: 3)}

\item{digits_tau}{Integer. Decimal places for heterogeneity (default: 3)}

\item{locale}{Character. Locale for decimal separator: "en" or "de"
(default: current pharmhand locale)}

\item{include_prob}{Logical. Include probability statements (default: TRUE)}

\item{include_interpretation}{Logical. Include text interpretation
(default: TRUE)}

\item{ci_brackets}{Character vector of length 2 for CI brackets
(default: \code{c("[", "]")} per IQWiG)}
}
\value{
A list with formatted strings for:
\item{estimate}{Formatted point estimate}
\item{ci}{Formatted credible interval as "lower; upper" with brackets}
\item{tau}{Formatted heterogeneity estimate}
\item{tau_ci}{Formatted heterogeneity CI}
\item{probability}{Probability statement string}
\item{interpretation}{Text interpretation}
\item{full_text}{Complete formatted summary text}
}
\description{
Formats a bayesian_meta_result object according to IQWiG Methods v8.0
requirements. Includes effect estimate, credible interval, heterogeneity,
and probability statements with proper formatting (semicolons in CIs,
specific decimal places, etc.)
}
\examples{
\dontrun{
yi <- log(c(0.75, 0.82))
sei <- c(0.12, 0.15)
result <- bayesian_meta_analysis(
  yi = yi, sei = sei, effect_measure = "hr"
)
formatted <- format_bayesian_result_iqwig(result)
print(formatted$full_text)
}
}
