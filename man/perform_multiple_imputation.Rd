% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{perform_multiple_imputation}
\alias{perform_multiple_imputation}
\title{Perform Multiple Imputation}
\usage{
perform_multiple_imputation(
  data,
  m = 5L,
  maxit = 5L,
  method = "pmm",
  predictorMatrix = NULL,
  seed = NULL,
  print = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame with missing values}

\item{m}{Integer. Number of imputations to perform (default: 5)}

\item{maxit}{Integer. Number of iterations for mice algorithm (default: 5)}

\item{method}{Character. Imputation method(s). Can be a single method applied
to all variables or a named vector specifying method per variable. Common
methods include:
\itemize{
\item "pmm": Predictive mean matching (default, for numeric)
\item "logreg": Logistic regression (for binary)
\item "polyreg": Polytomous regression (for unordered factors)
\item "polr": Proportional odds model (for ordered factors)
\item "norm": Bayesian linear regression
\item "rf": Random forest
}}

\item{predictorMatrix}{Matrix specifying which variables predict which.
If NULL (default), mice determines automatically.}

\item{seed}{Integer. Random seed for reproducibility}

\item{print}{Logical. Whether to print mice progress (default: FALSE)}

\item{...}{Additional arguments passed to mice::mice()}
}
\value{
An ImputationResult object
}
\description{
Performs multiple imputation using the mice package.
}
\details{
This function wraps mice::mice() to perform multiple imputation under the
Missing at Random (MAR) assumption. The mice algorithm uses chained
equations (MICE/FCS) to impute missing values.

For clinical trial data, predictive mean matching ("pmm") is often
recommended as it preserves the distribution of observed values and works
well with continuous variables.
}
\examples{
\dontrun{
# Basic multiple imputation
data <- data.frame(
  age = c(45, NA, 52, 38, NA),
  bmi = c(25, 28, NA, 31, 27),
  treatment = c("A", "B", "A", NA, "B")
)

result <- perform_multiple_imputation(data, m = 5, seed = 123)

# Access completed datasets
completed <- get_complete_data(result)

# View imputation summary
result@imputed_vars
result@n_missing
}
}
\references{
van Buuren, S. and Groothuis-Oudshoorn, K. (2011). mice: Multivariate
Imputation by Chained Equations in R. Journal of Statistical Software,
45(3), 1-67.
}
