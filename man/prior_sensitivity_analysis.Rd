% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_bayesian.R
\name{prior_sensitivity_analysis}
\alias{prior_sensitivity_analysis}
\title{Prior Sensitivity Analysis for Bayesian Meta-Analysis}
\usage{
prior_sensitivity_analysis(
  yi,
  sei,
  study_labels = NULL,
  effect_measure = c("hr", "or", "rr", "rd", "md", "smd"),
  prior_scenarios = NULL,
  chains = 2,
  iter = 2000,
  seed = 42,
  ...
)
}
\arguments{
\item{yi}{Numeric vector of effect estimates}

\item{sei}{Numeric vector of standard errors}

\item{study_labels}{Character vector of study names (optional)}

\item{effect_measure}{Character. Effect type}

\item{prior_scenarios}{List of prior specification scenarios. Each scenario
should have:
\code{name}: Character name for scenario
\code{prior_mu}: List with \code{mean} and \code{sd}
\code{prior_tau}: List with \code{type} and \code{scale}}

\item{chains}{Integer. Number of MCMC chains per scenario. Default: 2}

\item{iter}{Integer. Total iterations per chain. Default: 2000}

\item{seed}{Integer. Random seed for reproducibility}

\item{...}{Additional arguments passed to bayesian_meta_analysis()}
}
\value{
A list containing:
\item{scenarios}{List of bayesian_meta_result objects for each scenario}
\item{comparison}{Data frame comparing estimates across scenarios}
\item{sensitivity_summary}{Summary of how results change across priors}
}
\description{
Assess how sensitive results are to different prior specifications
}
\examples{
\dontrun{
scenarios <- list(
  weak = list(
    prior_mu = list(mean = 0, sd = 10),
    prior_tau = list(type = "half_cauchy", scale = 0.5)
  ),
  informative = list(
    prior_mu = list(mean = 0, sd = 1),
    prior_tau = list(type = "half_cauchy", scale = 0.25)
  )
)

sensitivity <- prior_sensitivity_analysis(
  yi = yi,
  sei = sei,
  effect_measure = "hr",
  prior_scenarios = scenarios
)
print(sensitivity$comparison)
}
}
