% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmst.R
\name{rmst_analysis}
\alias{rmst_analysis}
\title{RMST Analysis}
\usage{
rmst_analysis(
  data,
  time_var,
  event_var,
  trt_var,
  tau,
  conf_level = 0.95,
  reference_group = NULL
)
}
\arguments{
\item{data}{A data frame containing survival data}

\item{time_var}{Character. Name of the time variable}

\item{event_var}{Character. Name of the event indicator variable (1=event, 0=censored)}

\item{trt_var}{Character. Name of the treatment variable}

\item{tau}{Numeric. Time restriction for RMST calculation}

\item{conf_level}{Numeric. Confidence level for intervals (default: 0.95)}

\item{reference_group}{Character. Reference group for treatment comparison.
If NULL, uses first level of treatment variable.}
}
\value{
An RMSTResult object
}
\description{
Calculates restricted mean survival time with treatment group comparisons.
}
\details{
The restricted mean survival time (RMST) is defined as:
RMST(τ) = E\link{min(T, τ)} = ∫₀^τ S(t) dt

where T is the survival time and S(t) is the survival function.

RMST is estimated using the Kaplan-Meier estimator:
RMST(τ) = τ - ∫₀^τ F(t) dt = Σᵢ (tᵢ - tᵢ₋₁) * S(tᵢ₋₁)

where the integral is restricted to \link{0, τ}.
}
\examples{
\dontrun{
# Basic RMST analysis
result <- rmst_analysis(
  data = survival_data,
  time_var = "time",
  event_var = "status",
  trt_var = "TRT01P",
  tau = 12
)

# With custom confidence level
result <- rmst_analysis(
  data = survival_data,
  time_var = "time",
  event_var = "status",
  trt_var = "TRT01P",
  tau = 24,
  conf_level = 0.90
)
}
}
\references{
Uno, H. et al. (2014). Moving beyond the hazard ratio in quantifying the
between-group difference in survival analysis. Journal of Clinical Oncology,
32(22), 2380-2385.
}
