% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efficacy_tables.R
\name{detect_floor_ceiling}
\alias{detect_floor_ceiling}
\title{Detect Floor and Ceiling Effects}
\usage{
detect_floor_ceiling(
  data,
  score_var,
  min_score,
  max_score,
  by_var = NULL,
  threshold = 0.15,
  subject_var = "USUBJID"
)
}
\arguments{
\item{data}{Data frame containing PRO scores}

\item{score_var}{Character. Name of the score variable}

\item{min_score}{Numeric. Minimum possible score on the scale}

\item{max_score}{Numeric. Maximum possible score on the scale}

\item{by_var}{Character vector. Variables to group by (e.g.,
c("VISIT", "TRT01P"))}

\item{threshold}{Numeric. Threshold for flagging (default: 0.15 = 15\%)}

\item{subject_var}{Character. Subject identifier (default: "USUBJID")}
}
\value{
A data frame with columns:
\itemize{
\item Grouping variables
\item n: number of subjects with non-missing scores
\item n_floor: count of subjects at minimum
\item pct_floor: percentage at minimum
\item floor_flag: TRUE if pct_floor > threshold
\item n_ceiling: count of subjects at maximum
\item pct_ceiling: percentage at maximum
\item ceiling_flag: TRUE if pct_ceiling > threshold
}
}
\description{
Identifies floor/ceiling effects in PRO data when >15\% of responses
are at the minimum or maximum possible scale values.
}
