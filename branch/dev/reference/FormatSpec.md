# FormatSpec S7 Class

Internal class for storing format specifications.

## Usage

``` r
FormatSpec(
  pattern = character(0),
  null_format = "--",
  empty_format = "",
  neg_format = "sign",
  int_width = NULL,
  dec_width = NULL,
  auto_int = FALSE,
  auto_dec = FALSE,
  dec_adjust = 0L
)
```
