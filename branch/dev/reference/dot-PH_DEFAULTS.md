# Package-wide default values

Central registry of default parameter values used across the package.
These can be overridden using options("pharmhand.", value).

## Usage

``` r
.PH_DEFAULTS
```

## Format

An object of class `list` of length 10.
