# Parse configuration from YAML list

Internal function to parse YAML configuration list into
ConfigurationRegistry object.

## Usage

``` r
parse_config_registry(config)
```

## Arguments

- config:

  List from yaml::read_yaml()

## Value

A ConfigurationRegistry object
