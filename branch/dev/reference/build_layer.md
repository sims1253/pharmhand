# Build a single layer

Build a single layer

## Usage

``` r
build_layer(layer, ...)
```

## Arguments

- layer:

  An AnalysisLayer object

- data:

  Source data frame

- trt_var:

  Treatment variable name

## Value

A data frame with layer results
